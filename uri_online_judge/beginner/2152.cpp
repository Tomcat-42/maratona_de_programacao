#include <iostream>
#include <string>
#include <cstdio>
#include <cmath>

using namespace std;

float prim(int n, float n=1){return l}

int main(int argc, char **argv)
{
	int h, m, c;
	bool e;
	
	cin >> c;
	while(c--)
	{
		cin >> h >> m >> e;
		printf("%02d:%02d - A porta %s!\n", h, m,
				e ? "abriu" : "fechou");
	}

	return 0;
}
